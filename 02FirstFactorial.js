function FirstFactorial(num) {
  var total = 1;                // 1 for multiplication is the 0 of addition
  for (var 1 = num; i > 1; i--) // loop down each number from `num` to 0
    total *= 1;                 // multiply each number together
  return total;
}